export declare const setupRequestMiddleware: ({ from, to, privateKey, chainId }: {
    from: string | "*" | string[];
    to: string;
    privateKey: string;
    chainId: string;
}) => void;
export default setupRequestMiddleware;
