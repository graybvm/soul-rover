import { Express } from "express";
import { PromptFunction } from "../types";
import { Request, Response } from "express";
interface ServerOptions {
    staticDir?: string;
    staticUrlPath?: string;
    enableStaticServing?: boolean;
    logger?: boolean;
    isStreamSupported?: boolean;
    extendedRoutes?: {
        path: string;
        method: "GET" | "POST" | "PUT" | "DELETE";
        handler: (req: Request, res: Response) => void;
    }[];
}
export declare const startServer: (port: number, prompt: PromptFunction, options?: ServerOptions, callback?: ((error?: Error | undefined) => void) | undefined) => Express;
export {};
